# Database Provider Configuration
# Choose one: sqlite, postgresql, mysql
DATABASE_PROVIDER=sqlite

# Database URLs for different providers
# SQLite (default)
DATABASE_URL="file:./dev.db"

# PostgreSQL
# DATABASE_URL="postgresql://username:password@localhost:5432/tracking_progress"

# MySQL
# DATABASE_URL="mysql://username:password@localhost:3306/tracking_progress"

# JWT Configuration
JWT_SECRET=
JWT_EXPIRES_IN=
JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRES_IN=

# Server Configuration
PORT=
